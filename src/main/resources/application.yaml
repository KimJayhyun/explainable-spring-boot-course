spring:
  application:
    name: hello-spring

  datasource:
    # url: jdbc:h2:mem:testdb
    url: jdbc:h2:./data/dem # 파일 기반
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
